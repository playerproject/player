
# Instantiate the example driver, which supports the position interface

driver
(
  name "exampledriver"
  plugin "libexampledriver.so"
  # if you build the plugin with libtool, use this line instead:
  #plugin "exampledriver.la"
  provides ["position:0"] 
  foo 1 
)

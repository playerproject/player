IF (PLAYER_OS_WIN)
    INSTALL (FILES rpc/xdr.h rpc/types.h DESTINATION ${PLAYER_INCLUDE_INSTALL_DIR}/rpc)
    INSTALL (FILES replace.h DESTINATION ${PLAYER_INCLUDE_INSTALL_DIR})
ENDIF (PLAYER_OS_WIN)


PLAYERDRIVER_OPTION (stoc build_stoc ON)
PLAYERDRIVER_REQUIRE_HEADER_CPP (stoc build_stoc SVS/svsclass.h)
IF (PTHREAD_LIB)
    SET (PTHREAD_LIB_FLAG "-l${PTHREAD_LIB}")
ENDIF (PTHREAD_LIB)
PLAYERDRIVER_ADD_DRIVER (stoc build_stoc LINKFLAGS -lsvscap -lsvs -lsvscalc ${PTHREAD_LIB_FLAG} -ldcap -lraw1394 SOURCES stoc.cc)
